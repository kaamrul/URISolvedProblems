#include<stdio.h>

int main()
{
   int A, B, X;
   scanf("%d %d",&A,&B); //take input for A and B
   X = A + B;                //Basic summation X = A + B
   printf("X = %d\n", X); //Hardly advised to give \n at last
   return 0;
}
